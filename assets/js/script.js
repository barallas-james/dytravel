// When the page isn't loaded yet, preloader shows
$(window).on('load',function() {
    $(".preloader").fadeOut(1000);
})
// When the page is ready, the preloader will fade and the page will show
$(document).ready(function() {

});